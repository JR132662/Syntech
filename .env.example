# Optional: base URL for share links, Open Graph, canonical URL, sitemap, and robots.
# Set this when your custom domain is linked (e.g. https://syntechturf.com).
# If unset: on Vercel we use the deployment URL; locally we use http://localhost:3000.
# NEXT_PUBLIC_SITE_URL=https://syntechturf.com
